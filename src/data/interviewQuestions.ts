// ============================================================
// Interview Questions — sourced from:
// https://github.com/brijesh2501/interview-questions
// Curated real interview questions (2026) for Fullstack developers
// TODO: Replace JSON with API call when backend is ready
// ============================================================
import type { InterviewQuestion } from "../types";

export const interviewQuestions: InterviewQuestion[] = [
  // =====================================================
  // NODE.JS
  // =====================================================
  {
    id: "node-1",
    category: "Node.js",
    question: "Explain Callstack, Event Loop, Thread Pool, Task/Callback Queue, Microtask Queue, and Web API in Node.js.",
    answer: "The Call Stack executes synchronous code (LIFO). When async operations (timers, I/O) are encountered, they're handed off to the Thread Pool (libuv) or OS kernel. On completion, callbacks go to the Task Queue (macrotasks like setTimeout) or Microtask Queue (Promises, process.nextTick). The Event Loop continuously checks: if the Call Stack is empty, it first drains the Microtask Queue, then picks one task from the Task Queue and pushes it onto the Call Stack. Web APIs (in browsers) or C++ bindings (in Node) handle the actual async work.",
    difficulty: "Medium",
    tags: ["nodejs", "event-loop", "architecture"],
  },
  {
    id: "node-2",
    category: "Node.js",
    question: "How to handle 1 million concurrent requests using Node.js?",
    answer: "Use Node's non-blocking I/O with the cluster module to spawn workers across all CPU cores. Employ a reverse proxy (Nginx) for load balancing. Use connection pooling for databases, implement caching (Redis), compress responses (gzip), and use streams for large payloads. Avoid synchronous operations in the hot path. Consider horizontal scaling with PM2 or Kubernetes for orchestration.",
    difficulty: "Hard",
    tags: ["nodejs", "scaling", "performance"],
  },
  {
    id: "node-3",
    category: "Node.js",
    question: "What are Streams and Buffers in Node.js?",
    answer: "Buffers are fixed-size chunks of raw binary data in memory (useful for handling binary data like files, network packets). Streams are abstract interfaces for reading/writing data piece by piece instead of loading everything into memory. There are 4 types: Readable (fs.createReadStream), Writable (fs.createWriteStream), Duplex (TCP sockets), and Transform (zlib compression). Streams use events like 'data', 'end', 'error' and can be piped together.",
    difficulty: "Medium",
    tags: ["nodejs", "streams", "buffers"],
  },
  {
    id: "node-4",
    category: "Node.js",
    question: "How to create custom middleware in Node.js (Express)?",
    answer: "Middleware is a function with (req, res, next) signature. Create it as: `const logger = (req, res, next) => { console.log(req.method, req.url); next(); };` Apply globally with `app.use(logger)` or per-route with `app.get('/api', logger, handler)`. Always call `next()` to pass control to the next middleware, or send a response to end the cycle. For error middleware, use 4 parameters: `(err, req, res, next)`.",
    difficulty: "Easy",
    tags: ["nodejs", "express", "middleware"],
  },
  {
    id: "node-5",
    category: "Node.js",
    question: "How do you handle CPU-intensive tasks in Node.js?",
    answer: "Since Node.js is single-threaded, CPU-heavy tasks block the event loop. Solutions: 1) Worker Threads (worker_threads module) for parallel JS execution, 2) Child Processes (child_process.fork) to spawn separate Node processes, 3) Offload to a job queue (Bull/BullMQ with Redis) processed by separate workers, 4) Use native C++ addons via N-API for compute-heavy algorithms, 5) Use a microservice in a language better suited for CPU work (Go, Rust).",
    difficulty: "Hard",
    tags: ["nodejs", "performance", "worker-threads"],
  },
  {
    id: "node-6",
    category: "Node.js",
    question: "How do you manage high CPU on a server running Node.js?",
    answer: "Monitor with tools like `top`, `htop`, or Node's `--prof` flag. Profile with Chrome DevTools (node --inspect). Identify hot functions. Use cluster module to spread load across cores. Offload CPU work to Worker Threads or child processes. Implement caching to reduce repeated computations. Use a reverse proxy (Nginx) to handle static assets. Set up auto-scaling (horizontal) if on cloud infrastructure. Optimize algorithms and avoid synchronous fs/crypto operations.",
    difficulty: "Medium",
    tags: ["nodejs", "cpu", "debugging"],
  },
  {
    id: "node-7",
    category: "Node.js",
    question: "How do you create indexing in MongoDB?",
    answer: "`db.collection.createIndex({ field: 1 })` for ascending, `-1` for descending. Compound index: `createIndex({ name: 1, age: -1 })`. Text index: `createIndex({ description: 'text' })`. Unique index: `createIndex({ email: 1 }, { unique: true })`. Use `explain('executionStats')` to verify index usage. Indexes speed up reads but slow writes. Use the ESR rule (Equality, Sort, Range) for optimal compound index field ordering.",
    difficulty: "Medium",
    tags: ["nodejs", "mongodb", "database"],
  },
  {
    id: "node-8",
    category: "Node.js",
    question: "How do you design a scalable schema using MongoDB?",
    answer: "Follow these principles: 1) Model data based on access patterns (not normalized like SQL), 2) Embed related data that's always fetched together (denormalization), 3) Use references for large/frequently-updated subdocuments, 4) Avoid unbounded arrays, 5) Use bucket pattern for time-series data, 6) Use polymorphic pattern for varying document shapes, 7) Plan for sharding early — choose a good shard key (high cardinality, even distribution), 8) Use indexes aligned with query patterns.",
    difficulty: "Hard",
    tags: ["nodejs", "mongodb", "schema-design"],
  },
  {
    id: "node-9",
    category: "Node.js",
    question: "What is the CAP Theorem?",
    answer: "CAP states a distributed system can guarantee only 2 of 3: Consistency (all nodes see same data at same time), Availability (every request gets a response), Partition Tolerance (system works despite network failures). Since network partitions are inevitable, you choose between CP (e.g., MongoDB, HBase — consistent but may be unavailable during partitions) or AP (e.g., Cassandra, DynamoDB — always available but eventually consistent).",
    difficulty: "Medium",
    tags: ["nodejs", "distributed-systems", "cap"],
  },
  {
    id: "node-10",
    category: "Node.js",
    question: "Why use Kafka instead of SQL for messaging?",
    answer: "Kafka is a distributed event streaming platform designed for high-throughput, real-time data pipelines. Unlike SQL (designed for CRUD), Kafka: 1) Handles millions of events/sec with low latency, 2) Provides durable, ordered, append-only logs, 3) Supports publish-subscribe and consumer groups, 4) Retains messages for replay (time-based retention), 5) Decouples producers/consumers for microservices, 6) Enables event sourcing and CQRS patterns. SQL databases aren't built for high-throughput event streaming.",
    difficulty: "Hard",
    tags: ["nodejs", "kafka", "architecture"],
  },
  {
    id: "node-11",
    category: "Node.js",
    question: "How to build a Docker image (and its dependencies)?",
    answer: "Create a Dockerfile: `FROM node:20-alpine` → `WORKDIR /app` → `COPY package*.json ./` → `RUN npm ci --only=production` → `COPY . .` → `EXPOSE 3000` → `CMD [\"node\", \"src/index.js\"]`. Dependencies are NOT automatic — you must COPY package.json and run npm install. Use multi-stage builds to keep images small. Use `.dockerignore` to exclude node_modules, .git, etc. Build with `docker build -t myapp .` and run with `docker run -p 3000:3000 myapp`.",
    difficulty: "Medium",
    tags: ["nodejs", "docker", "devops"],
  },
  {
    id: "node-12",
    category: "Node.js",
    question: "Explain Sharding and Partitioning.",
    answer: "Partitioning splits a table's data into smaller, more manageable pieces within the same database instance (horizontal partitioning = rows, vertical = columns). Sharding is horizontal partitioning distributed across multiple database servers. Each shard holds a subset of data based on a shard key. Benefits: better query performance, horizontal scaling, improved write throughput. Challenges: cross-shard queries are expensive, rebalancing shards, choosing the right shard key.",
    difficulty: "Hard",
    tags: ["nodejs", "database", "scaling"],
  },
  {
    id: "node-13",
    category: "Node.js",
    question: "Horizontal vs Vertical Scaling — what's the difference?",
    answer: "Vertical Scaling (Scale Up): Add more CPU, RAM, storage to existing server. Simpler but has hardware limits and a single point of failure. Horizontal Scaling (Scale Out): Add more server instances behind a load balancer. More complex (needs stateless design, shared sessions, distributed caching) but offers near-unlimited scaling, fault tolerance, and no single point of failure. Node.js cluster module is vertical; Kubernetes pods are horizontal.",
    difficulty: "Easy",
    tags: ["nodejs", "scaling", "architecture"],
  },
  {
    id: "node-14",
    category: "Node.js",
    question: "What is a child process in Node.js?",
    answer: "The `child_process` module allows spawning subprocesses. Four methods: `exec()` (runs shell command, buffers output), `execFile()` (runs executable directly), `spawn()` (streams I/O, best for long-running processes), `fork()` (special spawn for Node scripts with IPC channel for message passing). fork() is commonly used to offload CPU-intensive work. Child processes run in separate memory spaces and communicate via IPC (Inter-Process Communication).",
    difficulty: "Medium",
    tags: ["nodejs", "child-process", "concurrency"],
  },
  {
    id: "node-15",
    category: "Node.js",
    question: "Explain Singleton, Factory, and Observer design patterns.",
    answer: "Singleton: Ensures only one instance exists (e.g., database connection pool). In Node, modules are cached so `module.exports = new DbPool()` is effectively a singleton. Factory: Creates objects without specifying exact class. A function returns different objects based on input: `createUser(type)` returns Admin or Student. Observer: Subjects notify observers on state changes. Node's EventEmitter is the built-in observer pattern — `emitter.on('event', handler)` and `emitter.emit('event', data)`.",
    difficulty: "Medium",
    tags: ["nodejs", "design-patterns"],
  },
  {
    id: "node-16",
    category: "Node.js",
    question: "How do you handle 404 (route not found) in Node.js/Express?",
    answer: "Add a catch-all middleware AFTER all route definitions: `app.use((req, res) => { res.status(404).json({ error: 'Route not found' }); });`. This runs only when no previous route matched. For HTML apps, serve a custom 404 page. Place it before the global error handler. In Express 5, you can also use `app.all('*splat', handler)` as a catch-all.",
    difficulty: "Easy",
    tags: ["nodejs", "express", "error-handling"],
  },

  // =====================================================
  // REACT.JS
  // =====================================================
  {
    id: "react-1",
    category: "React",
    question: "Controlled vs Uncontrolled components — what's the difference?",
    answer: "Controlled: Form element value is driven by React state. `<input value={name} onChange={e => setName(e.target.value)} />`. React is the single source of truth. Uncontrolled: Form element maintains its own internal state. You read values via refs: `<input ref={inputRef} />` then `inputRef.current.value`. Controlled gives more control (validation, formatting), uncontrolled is simpler for basic forms or integrating non-React libraries.",
    difficulty: "Easy",
    tags: ["react", "forms", "fundamentals"],
  },
  {
    id: "react-2",
    category: "React",
    question: "Static site generation vs Server-side rendering — what's the difference?",
    answer: "SSG (Static Site Generation): HTML is generated at build time. Pages are pre-rendered and served from a CDN. Best for content that doesn't change often (blogs, docs). SSR (Server-Side Rendering): HTML is generated on each request on the server. Better for dynamic, personalized content. SSG is faster (no server processing per request) but SSR shows fresher data. Next.js supports both: `getStaticProps` for SSG, `getServerSideProps` for SSR.",
    difficulty: "Medium",
    tags: ["react", "ssr", "ssg"],
  },
  {
    id: "react-3",
    category: "React",
    question: "How do you manage state in React?",
    answer: "Local state: useState/useReducer for component-level state. Lifted state: share state via closest common parent. Context API: for global state without prop drilling (theme, auth, locale). External libraries: Redux/Zustand/Jotai for complex app-wide state with middleware, devtools, persistence. Server state: React Query/TanStack Query for caching, synchronization, and deduplication of API data. URL state: useSearchParams for filter/pagination state.",
    difficulty: "Medium",
    tags: ["react", "state-management"],
  },
  {
    id: "react-4",
    category: "React",
    question: "How to avoid prop drilling in React?",
    answer: "1) Context API: Create context with createContext, provide value via Provider, consume with useContext hook. 2) State management libraries: Redux, Zustand, Jotai provide global stores accessible anywhere. 3) Component composition: Pass components as children/render props to avoid passing data through intermediate layers. 4) Custom hooks: Encapsulate shared logic and state. Context is best for low-frequency updates; external stores for high-frequency updates.",
    difficulty: "Easy",
    tags: ["react", "prop-drilling", "state"],
  },
  {
    id: "react-5",
    category: "React",
    question: "Context API vs Redux — especially regarding re-renders?",
    answer: "Context API: Any change to context value re-renders ALL consumers, even if they only use a subset of the value. Split contexts or memoize to mitigate. Redux: Components subscribe to specific slices via selectors (`useSelector`). Only re-renders when the selected slice changes. Redux also offers middleware (thunk/saga), devtools time-travel, and predictable state updates via reducers. Use Context for simple/infrequent state (theme, auth); Redux for complex, frequently-changing app state.",
    difficulty: "Medium",
    tags: ["react", "redux", "context-api"],
  },
  {
    id: "react-6",
    category: "React",
    question: "React.memo vs useMemo — what's the difference?",
    answer: "React.memo is a HOC that memoizes a component — it skips re-rendering if props haven't changed (shallow comparison). `export default React.memo(MyComponent)`. useMemo is a hook that memoizes a computed value inside a component: `const sorted = useMemo(() => expensiveSort(items), [items])`. Use React.memo to prevent unnecessary component re-renders; use useMemo to prevent expensive recalculations on every render.",
    difficulty: "Medium",
    tags: ["react", "performance", "hooks"],
  },
  {
    id: "react-7",
    category: "React",
    question: "useEffect vs useLayoutEffect — what's the difference?",
    answer: "useEffect runs asynchronously AFTER the browser paints the screen. Good for data fetching, subscriptions, logging. useLayoutEffect runs synchronously AFTER DOM mutations but BEFORE the browser paints. Good for measuring DOM elements (getBoundingClientRect), adjusting layout, or preventing visual flicker. useLayoutEffect blocks painting, so overuse causes jank. 99% of the time useEffect is correct.",
    difficulty: "Medium",
    tags: ["react", "hooks", "lifecycle"],
  },
  {
    id: "react-8",
    category: "React",
    question: "useReducer vs useCallback vs useMemo — when to use each?",
    answer: "useReducer: Alternative to useState for complex state logic with multiple sub-values or when next state depends on previous. Uses a reducer function (state, action) => newState. useCallback: Memoizes a function reference so it doesn't get recreated on every render. `useCallback(fn, [deps])` — useful when passing callbacks to memoized child components. useMemo: Memoizes a computed value. `useMemo(() => compute(a), [a])` — useful for expensive calculations.",
    difficulty: "Medium",
    tags: ["react", "hooks", "performance"],
  },
  {
    id: "react-9",
    category: "React",
    question: "useRef in React vs DOM manipulation in JavaScript — what's the difference?",
    answer: "useRef creates a mutable ref object (`{ current: value }`) that persists across renders without causing re-renders. Used for: accessing DOM elements (`ref={myRef}` then `myRef.current`), storing mutable values (prev state, timers). Direct DOM manipulation (`document.getElementById`) bypasses React's virtual DOM, can cause inconsistencies, and is harder to maintain. useRef is React-idiomatic — it integrates with the component lifecycle.",
    difficulty: "Easy",
    tags: ["react", "refs", "dom"],
  },
  {
    id: "react-10",
    category: "React",
    question: "What are React Portals?",
    answer: "Portals render children into a DOM node outside the parent component's DOM hierarchy while preserving React's event bubbling. Created via `ReactDOM.createPortal(children, domNode)`. Common use cases: modals, tooltips, popovers, toasts — elements that need to visually break out of parent overflow:hidden or z-index stacking contexts. Events still bubble through the React tree (not the DOM tree).",
    difficulty: "Medium",
    tags: ["react", "portals", "dom"],
  },
  {
    id: "react-11",
    category: "React",
    question: "What is React Virtualization (react-window, react-virtualized)?",
    answer: "Virtualization renders only the visible items in a large list/table, significantly improving performance. Instead of rendering 10,000 DOM nodes, only ~20 visible items are rendered. As you scroll, items are recycled. react-window (lightweight, by the same author) and react-virtualized (full-featured) are popular libraries. Key components: FixedSizeList, VariableSizeList, VirtualizedTable. Essential for large datasets in dashboards, data grids, and infinite scroll UIs.",
    difficulty: "Medium",
    tags: ["react", "performance", "virtualization"],
  },
  {
    id: "react-12",
    category: "React",
    question: "How do you unmount a component in functional React?",
    answer: "Return a cleanup function from useEffect: `useEffect(() => { /* setup */ return () => { /* cleanup — runs on unmount */ }; }, []);`. The empty dependency array ensures it runs once on mount and the cleanup runs on unmount. Use this to cancel subscriptions, clear timers, abort fetch requests (`AbortController`), or remove event listeners. In React 18 Strict Mode (dev), effects mount → unmount → mount to help find bugs.",
    difficulty: "Easy",
    tags: ["react", "hooks", "lifecycle"],
  },
  {
    id: "react-13",
    category: "React",
    question: "How many Virtual DOMs does React use?",
    answer: "React maintains TWO virtual DOM trees: 1) The current tree (represents what's currently rendered on screen), 2) The work-in-progress tree (represents the next render). When state changes, React builds the new tree, diffs it against the current tree (reconciliation), calculates the minimal set of DOM operations needed, and batch-applies them to the real DOM. This is called the Fiber reconciliation algorithm (React 16+).",
    difficulty: "Medium",
    tags: ["react", "virtual-dom", "internals"],
  },
  {
    id: "react-14",
    category: "React",
    question: "How do you write Jest unit tests for button render and API response in React?",
    answer: "Button render: `render(<MyButton />); expect(screen.getByRole('button', { name: /submit/i })).toBeInTheDocument();`. API test: Mock axios/fetch, then `render(<UserList />); await waitFor(() => expect(screen.getByText('John')).toBeInTheDocument());`. Use @testing-library/react for rendering, jest.mock or MSW for API mocking, userEvent for interactions: `await userEvent.click(screen.getByRole('button'));`.",
    difficulty: "Medium",
    tags: ["react", "testing", "jest"],
  },
  {
    id: "react-15",
    category: "React",
    question: "How do you secure React applications?",
    answer: "1) Sanitize user input to prevent XSS (React auto-escapes JSX but dangerouslySetInnerHTML is risky), 2) Use HTTPS everywhere, 3) Store tokens in httpOnly cookies (not localStorage), 4) Implement CSRF tokens, 5) Use Content-Security-Policy headers, 6) Validate/sanitize on the server (never trust client), 7) Keep dependencies updated (npm audit), 8) Use environment variables for secrets (never bundle them), 9) Implement proper CORS, 10) Use auth libraries (Auth0, NextAuth).",
    difficulty: "Hard",
    tags: ["react", "security", "best-practices"],
  },
  {
    id: "react-16",
    category: "React",
    question: "How do you debug React apps (e.g., login failure)?",
    answer: "1) React DevTools: Inspect component tree, props, state, hooks, profiler for performance, 2) Browser DevTools: Network tab for API request/response, Console for errors, 3) Add console.log or breakpoints in the component and service layer, 4) Check the Network tab for 401/403 status codes, CORS errors, missing headers, 5) Verify token storage/retrieval logic, 6) Use React Error Boundaries to catch render errors, 7) For state issues, use Redux DevTools for time-travel debugging.",
    difficulty: "Easy",
    tags: ["react", "debugging"],
  },
  {
    id: "react-17",
    category: "React",
    question: "Difference between Axios and Fetch API?",
    answer: "Axios: Automatic JSON parsing, request/response interceptors, timeout support, request cancellation (CancelToken/AbortController), works in Node.js, automatic error handling (rejects on 4xx/5xx). Fetch: Built-in browser API (no install), uses Promises, does NOT reject on HTTP errors (must check res.ok), no timeout support natively, no interceptors, streaming support with ReadableStream. Axios is more feature-rich; Fetch is lighter and native.",
    difficulty: "Easy",
    tags: ["react", "axios", "fetch"],
  },
  {
    id: "react-18",
    category: "React",
    question: "How do you handle concurrency in React (e.g., useTransition)?",
    answer: "React 18 introduced concurrent features: useTransition marks state updates as non-urgent — React can interrupt rendering to keep the UI responsive. `const [isPending, startTransition] = useTransition(); startTransition(() => setFilter(value));`. useDeferredValue defers a value update. Suspense + lazy loading for code splitting. These features let React prioritize urgent updates (typing) over expensive ones (filtering a large list) without blocking the UI.",
    difficulty: "Hard",
    tags: ["react", "concurrency", "react18"],
  },
  {
    id: "react-19",
    category: "React",
    question: "What is the React Fiber Algorithm?",
    answer: "Fiber is React's reconciliation engine (React 16+). It replaced the old stack-based recursive reconciler with an incremental, interruptible one. Each component/element is a Fiber node (a JS object with type, props, state, child, sibling, return pointers). Key features: 1) Work can be paused and resumed (time-slicing), 2) Different priority levels for updates, 3) Ability to reuse previously completed work, 4) Ability to abort work. This enables concurrent rendering in React 18.",
    difficulty: "Hard",
    tags: ["react", "fiber", "internals"],
  },
  {
    id: "react-20",
    category: "React",
    question: "What are Higher Order Components (HOC)? Why and when to use them?",
    answer: "A HOC is a function that takes a component and returns a new enhanced component: `const Enhanced = withAuth(MyComponent)`. The HOC wraps the original, adding extra props, logic, or rendering behavior. Use cases: authentication guards, logging, theming, data fetching wrappers. In modern React, custom hooks often replace HOCs since they're simpler and don't create wrapper nesting. HOCs are still useful for cross-cutting concerns in class components.",
    difficulty: "Medium",
    tags: ["react", "hoc", "patterns"],
  },
  {
    id: "react-21",
    category: "React",
    question: "What is React Reconciliation?",
    answer: "Reconciliation is React's diffing algorithm that determines what changed between two renders. Rules: 1) Different element types produce different trees (unmount old, mount new), 2) Elements with stable `key` props are matched across renders, 3) React compares same-type elements and updates only changed attributes. Keys are crucial for lists — without them, React may re-order incorrectly. This O(n) heuristic algorithm (not O(n³) tree diff) makes React fast.",
    difficulty: "Medium",
    tags: ["react", "reconciliation", "virtual-dom"],
  },
  {
    id: "react-22",
    category: "React",
    question: "Throttling vs Debouncing — what's the difference?",
    answer: "Debouncing: Delays execution until a pause in events. E.g., search input — only fires API call 300ms after the user stops typing. `useEffect(() => { const timer = setTimeout(() => search(query), 300); return () => clearTimeout(timer); }, [query])`. Throttling: Ensures execution at most once per interval. E.g., scroll handler fires at most every 100ms. Debounce = wait for calm; Throttle = execute at regular intervals.",
    difficulty: "Medium",
    tags: ["react", "performance", "patterns"],
  },
  {
    id: "react-23",
    category: "React",
    question: "Redux Thunk vs Redux Saga middleware?",
    answer: "Redux Thunk: Simple — action creators return functions instead of objects. The function receives dispatch/getState. Good for straightforward async (API calls). `dispatch(async (dispatch) => { const data = await fetch(...); dispatch(setData(data)); })`. Redux Saga: Uses generator functions for complex async flows (race conditions, cancellation, retry, debounce, parallel calls). More powerful but steeper learning curve. Use Thunk for simple apps; Saga for complex async orchestration.",
    difficulty: "Hard",
    tags: ["react", "redux", "middleware"],
  },
  {
    id: "react-24",
    category: "React",
    question: "What is React Hydration?",
    answer: "Hydration is the process where React attaches event listeners and makes server-rendered HTML interactive on the client. When SSR sends pre-rendered HTML, the browser shows it immediately (fast FCP). Then React 'hydrates' — it walks the existing DOM, attaches handlers, and reconciles with its virtual DOM without re-creating DOM nodes. React 18 introduced Selective Hydration — it can hydrate parts of the page progressively, prioritizing what the user interacts with.",
    difficulty: "Hard",
    tags: ["react", "ssr", "hydration"],
  },
  {
    id: "react-25",
    category: "React",
    question: "What is Webpack and how to configure it?",
    answer: "Webpack is a module bundler that processes JS, CSS, images into optimized bundles. Config (webpack.config.js): `entry` (starting point), `output` (bundle destination), `module.rules` (loaders — babel-loader for JS/JSX, css-loader, file-loader), `plugins` (HtmlWebpackPlugin, MiniCssExtractPlugin), `devServer` for HMR. Modern alternatives: Vite (uses esbuild for dev, Rollup for production) is much faster. CRA used Webpack under the hood.",
    difficulty: "Medium",
    tags: ["react", "webpack", "build-tools"],
  },
  {
    id: "react-26",
    category: "React",
    question: "useEffect(() => {},[]) vs useEffect(() => {}) vs useEffect(() => {},[dependency]) — what's the difference?",
    answer: "`useEffect(() => {}, [])`: Runs ONCE after initial render (mount). Cleanup runs on unmount. Like componentDidMount. `useEffect(() => {})`: Runs after EVERY render (mount + every update). No dependency array = always re-run. Rarely desired. `useEffect(() => {}, [dep])`: Runs after initial render AND whenever `dep` changes. Cleanup runs before re-execution and on unmount. Most common pattern for data fetching based on changing props/state.",
    difficulty: "Easy",
    tags: ["react", "hooks", "useEffect"],
  },

  // =====================================================
  // JAVASCRIPT
  // =====================================================
  {
    id: "js-1",
    category: "JavaScript",
    question: "Explain ES6+ features: Spread, Rest, Arrow functions, Destructuring, Promises, Import, Class.",
    answer: "Spread (...): Expands array/object `[...arr, 4]`, `{...obj, key: val}`. Rest (...): Collects remaining args `function fn(...args)`. Arrow functions: `(a) => a*2` — lexical `this`, no own `arguments`. Destructuring: `const {name, age} = obj; const [a, b] = arr;`. Promises: `new Promise((resolve, reject) => {...}).then().catch()`. Import/Export: `import X from './mod'`, `export default`. Class: `class Dog extends Animal { constructor() { super(); } }`.",
    difficulty: "Easy",
    tags: ["javascript", "es6", "fundamentals"],
  },
  {
    id: "js-2",
    category: "JavaScript",
    question: "What is a Closure in JavaScript?",
    answer: "A closure is created when an inner function retains access to variables from its outer (enclosing) function's scope, even after the outer function has returned. Example: `function counter() { let count = 0; return () => ++count; } const inc = counter(); inc(); // 1, inc(); // 2`. The inner function 'closes over' the `count` variable. Used for data privacy, factory functions, partial application, and maintaining state in callbacks.",
    difficulty: "Medium",
    tags: ["javascript", "closures", "scope"],
  },
  {
    id: "js-3",
    category: "JavaScript",
    question: "What is Hoisting in JavaScript?",
    answer: "Hoisting moves declarations to the top of their scope during compilation. `var` is hoisted and initialized to `undefined` — accessible before declaration. `let`/`const` are hoisted but NOT initialized — accessing before declaration throws ReferenceError ('Temporal Dead Zone'). Function declarations are fully hoisted (name + body). Function expressions (`var fn = function(){}`) only hoist the variable (as undefined), not the function body.",
    difficulty: "Easy",
    tags: ["javascript", "hoisting", "scope"],
  },
  {
    id: "js-4",
    category: "JavaScript",
    question: "Rest vs Spread operator — what's the difference?",
    answer: "Same syntax (...) but different contexts. Spread EXPANDS: In function calls `Math.max(...arr)`, array literals `[...a, ...b]`, object literals `{...obj}` — it spreads elements/properties out. Rest COLLECTS: In function parameters `function fn(a, ...rest)` — collects remaining arguments into an array. In destructuring `const {a, ...rest} = obj` — collects remaining properties into an object. Spread takes things apart; Rest gathers things together.",
    difficulty: "Easy",
    tags: ["javascript", "es6", "operators"],
  },
  {
    id: "js-5",
    category: "JavaScript",
    question: "Explain call(), bind(), and apply() in JavaScript.",
    answer: "All three set the `this` context for a function. `call(thisArg, arg1, arg2)`: Invokes immediately with args listed individually. `apply(thisArg, [argsArray])`: Invokes immediately with args as an array. `bind(thisArg, arg1)`: Returns a NEW function with `this` bound (doesn't invoke). Example: `greet.call(user, 'Hi')` vs `greet.apply(user, ['Hi'])` vs `const bound = greet.bind(user); bound('Hi');`. bind is useful for event handlers and callbacks.",
    difficulty: "Medium",
    tags: ["javascript", "this", "functions"],
  },
  {
    id: "js-6",
    category: "JavaScript",
    question: "forEach vs map() — what's the difference?",
    answer: "`forEach`: Iterates over array, executes callback for side effects, returns undefined. Cannot be chained. `[1,2,3].forEach(x => console.log(x))`. `map`: Iterates and returns a NEW array with transformed values. Pure transformation, chainable. `[1,2,3].map(x => x*2) // [2,4,6]`. Use map when you need a transformed array; forEach when you just need side effects (logging, DOM updates). map is more functional-programming friendly.",
    difficulty: "Easy",
    tags: ["javascript", "arrays", "fundamentals"],
  },
  {
    id: "js-7",
    category: "JavaScript",
    question: "map vs filter vs reduce — explain each.",
    answer: "`map(fn)`: Transforms each element, returns new array of same length. `[1,2,3].map(x => x*2) // [2,4,6]`. `filter(fn)`: Returns new array with elements where fn returns truthy. `[1,2,3].filter(x => x > 1) // [2,3]`. `reduce(fn, init)`: Reduces array to a single value by accumulating. `[1,2,3].reduce((sum, x) => sum + x, 0) // 6`. All are immutable (return new arrays/values). They can be chained: `arr.filter(...).map(...).reduce(...)`.",
    difficulty: "Easy",
    tags: ["javascript", "arrays", "functional"],
  },
  {
    id: "js-8",
    category: "JavaScript",
    question: "Explain Currying in JavaScript (with and without nested functions).",
    answer: "Currying transforms a function with multiple arguments into a sequence of functions each taking one argument. With nesting: `const add = a => b => c => a + b + c; add(1)(2)(3); // 6`. Without nesting (generic curry): `function curry(fn) { return function curried(...args) { if (args.length >= fn.length) return fn(...args); return (...more) => curried(...args, ...more); }; }`. Enables partial application: `const add5 = add(5); add5(3)(2); // 10`.",
    difficulty: "Medium",
    tags: ["javascript", "currying", "functional"],
  },
  {
    id: "js-9",
    category: "JavaScript",
    question: "What is Memoization in JavaScript?",
    answer: "Memoization caches function results based on inputs, returning cached values for repeated calls. Example: `function memoize(fn) { const cache = new Map(); return (...args) => { const key = JSON.stringify(args); if (cache.has(key)) return cache.get(key); const result = fn(...args); cache.set(key, result); return result; }; }`. Useful for expensive computations (fibonacci, factorial). In React, useMemo and React.memo are forms of memoization.",
    difficulty: "Medium",
    tags: ["javascript", "memoization", "performance"],
  },
  {
    id: "js-10",
    category: "JavaScript",
    question: "Normal function vs Arrow function — what's the difference?",
    answer: "Arrow functions: 1) NO own `this` — inherits from enclosing scope (lexical `this`), 2) NO `arguments` object, 3) Cannot be used as constructors (no `new`), 4) No `prototype` property, 5) Cannot be used as generators. Regular functions: Have their own `this` (determined by how they're called), own `arguments` object, can be constructors. Arrow functions are ideal for callbacks; regular functions for methods and constructors.",
    difficulty: "Easy",
    tags: ["javascript", "functions", "es6"],
  },
  {
    id: "js-11",
    category: "JavaScript",
    question: "What is Event Bubbling and Event Delegation?",
    answer: "Event Bubbling: When an event fires on an element, it bubbles up through parent elements to the root. `<div onClick> <button onClick>` — clicking button triggers button's handler first, then div's. Event Delegation: Instead of attaching handlers to each child, attach ONE handler to a parent and use `event.target` to determine which child was clicked. Efficient for dynamic lists: `ul.addEventListener('click', e => { if (e.target.tagName === 'LI') handle(e.target); });`.",
    difficulty: "Medium",
    tags: ["javascript", "events", "dom"],
  },
  {
    id: "js-12",
    category: "JavaScript",
    question: "Deep copy vs Shallow copy in JavaScript?",
    answer: "Shallow copy: Copies top-level properties. Nested objects are still shared (same reference). Methods: `{...obj}`, `Object.assign({}, obj)`, `[...arr]`. Deep copy: Recursively copies all levels — no shared references. Methods: `structuredClone(obj)` (modern, handles circular refs), `JSON.parse(JSON.stringify(obj))` (loses functions, dates, undefined). Libraries: lodash `_.cloneDeep()`. Always deep copy when you need to mutate nested state independently.",
    difficulty: "Medium",
    tags: ["javascript", "copy", "objects"],
  },
  {
    id: "js-13",
    category: "JavaScript",
    question: "How to flatten a nested array in JavaScript (recursive and iterative)?",
    answer: "Built-in: `arr.flat(Infinity)`. Recursive: `function flatten(arr) { return arr.reduce((acc, val) => Array.isArray(val) ? acc.concat(flatten(val)) : acc.concat(val), []); }`. Iterative (stack): `function flatten(input) { const stack = [...input]; const result = []; while (stack.length) { const val = stack.pop(); Array.isArray(val) ? stack.push(...val) : result.push(val); } return result.reverse(); }`. Example: `flatten([1, [2, [3, [4]]]])  // [1,2,3,4]`.",
    difficulty: "Medium",
    tags: ["javascript", "arrays", "recursion"],
  },
  {
    id: "js-14",
    category: "JavaScript",
    question: "What is the Event Loop and how does it work?",
    answer: "The Event Loop enables non-blocking I/O in JavaScript's single-threaded runtime. Process: 1) Execute synchronous code on the Call Stack, 2) Async operations (timers, I/O) are handled by browser/Node APIs, 3) Completed callbacks enter Microtask Queue (Promises, queueMicrotask) or Macrotask Queue (setTimeout, setInterval, I/O), 4) Event Loop: When Call Stack is empty, drain ALL microtasks first, then pick ONE macrotask, repeat. Microtasks always run before macrotasks.",
    difficulty: "Hard",
    tags: ["javascript", "event-loop", "async"],
  },
  {
    id: "js-15",
    category: "JavaScript",
    question: "Console output: console.log([]==[]), let {length} = 'lyakat', console.log(3+'2'+5), console.log(1+true)?",
    answer: "`console.log([] == [])` → false (two different array references). `let {length} = 'lyakat'` → length is 6 (strings have a length property, destructured). `console.log(3+'2'+5)` → '325' (3+'2' = '32' string concatenation, then '32'+5 = '325'). `console.log(1+true)` → 2 (true coerces to 1). These test type coercion and reference comparison knowledge.",
    difficulty: "Medium",
    tags: ["javascript", "coercion", "tricky"],
  },

  // =====================================================
  // TYPESCRIPT
  // =====================================================
  {
    id: "ts-1",
    category: "TypeScript",
    question: "Difference between type and interface in TypeScript?",
    answer: "Both define object shapes but: Interface supports declaration merging (defining the same interface twice merges them). Type does NOT (duplicate type is an error). Interface uses `extends`, Type uses `&` (intersection). Type can represent unions `type A = string | number`, tuples, mapped types — Interface cannot. Interface is better for public APIs (extendable); Type is more flexible for complex type operations. Use interface for objects, type for unions/tuples.",
    difficulty: "Medium",
    tags: ["typescript", "types", "interface"],
  },
  {
    id: "ts-2",
    category: "TypeScript",
    question: "Generic type vs Specific type in TypeScript?",
    answer: "Specific: `function getFirst(arr: string[]): string` — works only with strings. Generic: `function getFirst<T>(arr: T[]): T` — works with ANY type while maintaining type safety. Generics provide reusability + type inference. The caller's type flows through: `getFirst([1,2,3])` returns `number`, `getFirst(['a','b'])` returns `string`. Use generics for utility functions, data structures, API responses, and anywhere you want type-safe reusability.",
    difficulty: "Medium",
    tags: ["typescript", "generics"],
  },
  {
    id: "ts-3",
    category: "TypeScript",
    question: "Find sum of digits of a number until it becomes single digit (digital root).",
    answer: "Recursive: `function digitalRoot(n: number): number { if (n < 10) return n; const sum = n.toString().split('').reduce((a, b) => a + parseInt(b), 0); return digitalRoot(sum); }`. Math shortcut: `n === 0 ? 0 : 1 + (n - 1) % 9` (digital root formula). Example: digitalRoot(1234) → 1+2+3+4=10 → 1+0=1. digitalRoot(5674) → 5+6+7+4=22 → 2+2=4.",
    difficulty: "Easy",
    tags: ["typescript", "coding", "recursion"],
  },

  // =====================================================
  // SQL
  // =====================================================
  {
    id: "sql-1",
    category: "SQL",
    question: "What are the ACID properties?",
    answer: "Atomicity: Transaction is all-or-nothing. If any part fails, the entire transaction rolls back. Consistency: Transaction moves database from one valid state to another, maintaining all constraints. Isolation: Concurrent transactions don't interfere with each other (levels: Read Uncommitted, Read Committed, Repeatable Read, Serializable). Durability: Once committed, data survives crashes (written to non-volatile storage). ACID ensures data integrity in relational databases.",
    difficulty: "Medium",
    tags: ["sql", "acid", "transactions"],
  },
  {
    id: "sql-2",
    category: "SQL",
    question: "What are CTEs (Common Table Expressions)?",
    answer: "CTEs are temporary named result sets within a single query using `WITH`. Syntax: `WITH cte_name AS (SELECT ...) SELECT * FROM cte_name;`. Benefits: improved readability, reusable within the same query, support recursive queries (`WITH RECURSIVE` for hierarchical data like org charts). Example recursive CTE: `WITH RECURSIVE emp AS (SELECT * FROM employees WHERE manager_id IS NULL UNION ALL SELECT e.* FROM employees e JOIN emp ON e.manager_id = emp.id) SELECT * FROM emp;`.",
    difficulty: "Medium",
    tags: ["sql", "cte", "queries"],
  },
  {
    id: "sql-3",
    category: "SQL",
    question: "Normalization vs Denormalization — what's the difference?",
    answer: "Normalization: Organizing data to reduce redundancy by dividing into related tables (1NF, 2NF, 3NF, BCNF). Reduces update anomalies but increases JOINs. Denormalization: Intentionally adding redundancy (duplicating data, precomputed columns) to improve read performance by reducing JOINs. Used in read-heavy systems, data warehouses, reporting. Trade-off: Normalization = data integrity + write efficiency; Denormalization = read performance + simpler queries.",
    difficulty: "Medium",
    tags: ["sql", "normalization", "design"],
  },
  {
    id: "sql-4",
    category: "SQL",
    question: "Explain 1NF, 2NF, and 3NF.",
    answer: "1NF: Each column has atomic (indivisible) values, no repeating groups. 2NF: Must be in 1NF + every non-key column fully depends on the ENTIRE primary key (no partial dependencies — relevant for composite keys). 3NF: Must be in 2NF + no transitive dependencies (non-key column depends on another non-key column). Example violation of 3NF: storing city AND zip_code — city depends on zip_code, not directly on the primary key.",
    difficulty: "Medium",
    tags: ["sql", "normalization", "fundamentals"],
  },
  {
    id: "sql-5",
    category: "SQL",
    question: "What are Views, Indexes, and Stored Procedures in SQL?",
    answer: "View: A virtual table based on a SELECT query. `CREATE VIEW active_users AS SELECT * FROM users WHERE active = 1;`. Provides abstraction and security. Index: Data structure (B-tree) that speeds up lookups on specific columns. `CREATE INDEX idx_email ON users(email);`. Speeds reads, slows writes. Stored Procedure: Precompiled SQL code block stored in the database. `CREATE PROCEDURE GetUser(@id INT) AS SELECT * FROM users WHERE id = @id;`. Reduces network roundtrips, enforces business logic.",
    difficulty: "Easy",
    tags: ["sql", "views", "indexes", "stored-procedures"],
  },
  {
    id: "sql-6",
    category: "SQL",
    question: "Explain all types of JOINs in SQL.",
    answer: "INNER JOIN: Returns only matching rows from both tables. LEFT JOIN: All rows from left + matched from right (NULLs for no match). RIGHT JOIN: All rows from right + matched from left. FULL OUTER JOIN: All rows from both (NULLs where no match). CROSS JOIN: Cartesian product (every row × every row). SELF JOIN: Table joined with itself (e.g., employee-manager). NATURAL JOIN: Auto-joins on common column names (avoid — fragile).",
    difficulty: "Easy",
    tags: ["sql", "joins", "fundamentals"],
  },
  {
    id: "sql-7",
    category: "SQL",
    question: "Clustered vs Non-clustered index — what's the difference?",
    answer: "Clustered: Physically reorders table data. Only ONE per table (usually on primary key). The table IS the index. Like a dictionary sorted alphabetically. Non-clustered: Separate structure with pointers to data rows. Multiple per table. Like a book's index at the back pointing to page numbers. Clustered is faster for range queries; non-clustered for lookups on non-primary columns. A table without a clustered index is called a 'heap'.",
    difficulty: "Medium",
    tags: ["sql", "indexes", "performance"],
  },
  {
    id: "sql-8",
    category: "SQL",
    question: "DELETE vs DROP table — what's the difference?",
    answer: "DELETE: DML command. Removes rows (can use WHERE). Table structure remains. Can be rolled back in a transaction. Triggers fire. Slow for large deletes (row-by-row logging). DROP: DDL command. Removes the ENTIRE table (structure + data + indexes + constraints). Cannot be rolled back (in most RDBMS). TRUNCATE: DDL-like, removes all rows but keeps structure. Faster than DELETE (minimal logging), resets identity. Cannot use WHERE.",
    difficulty: "Easy",
    tags: ["sql", "ddl", "dml"],
  },
  {
    id: "sql-9",
    category: "SQL",
    question: "How to debug slow SQL queries?",
    answer: "1) Use `EXPLAIN` / `EXPLAIN ANALYZE` to see query execution plan, 2) Check for full table scans — add indexes on filtered/joined columns, 3) Avoid SELECT * — fetch only needed columns, 4) Check for N+1 query patterns, 5) Optimize JOINs (ensure join columns are indexed), 6) Use query profiler / slow query log, 7) Check statistics are up to date (`ANALYZE TABLE`), 8) Consider denormalization or materialized views for complex aggregations, 9) Check for lock contention.",
    difficulty: "Hard",
    tags: ["sql", "performance", "debugging"],
  },
  {
    id: "sql-10",
    category: "SQL",
    question: "What are constraints in SQL?",
    answer: "Constraints enforce data integrity rules. PRIMARY KEY: Unique + NOT NULL identifier. FOREIGN KEY: References another table's primary key (referential integrity). UNIQUE: No duplicate values in the column. NOT NULL: Column cannot be null. CHECK: Custom condition `CHECK (age >= 18)`. DEFAULT: Sets default value if none provided. Constraints can be defined at column level or table level and can be named for easier management.",
    difficulty: "Easy",
    tags: ["sql", "constraints", "fundamentals"],
  },
  {
    id: "sql-11",
    category: "SQL",
    question: "Write a query to find the Nth highest salary.",
    answer: "Using ROW_NUMBER: `WITH ranked AS (SELECT salary, ROW_NUMBER() OVER (ORDER BY salary DESC) AS rn FROM employees GROUP BY salary) SELECT salary FROM ranked WHERE rn = N;`. Using subquery: `SELECT DISTINCT salary FROM employees e1 WHERE N-1 = (SELECT COUNT(DISTINCT salary) FROM employees e2 WHERE e2.salary > e1.salary);`. Using LIMIT/OFFSET: `SELECT DISTINCT salary FROM employees ORDER BY salary DESC LIMIT 1 OFFSET N-1;`.",
    difficulty: "Medium",
    tags: ["sql", "queries", "interview"],
  },
  {
    id: "sql-12",
    category: "SQL",
    question: "SQL vs NoSQL — when to use each?",
    answer: "SQL (Relational): Structured data, complex relationships, ACID transactions, strong consistency (banking, ERP, CRM). Examples: PostgreSQL, MySQL, SQL Server. NoSQL: Flexible schema, horizontal scaling, high throughput, eventual consistency. Document (MongoDB) for nested JSON, Key-Value (Redis) for caching, Column-family (Cassandra) for time-series, Graph (Neo4j) for relationships. Choose SQL for complex queries/transactions; NoSQL for scalability and schema flexibility.",
    difficulty: "Easy",
    tags: ["sql", "nosql", "comparison"],
  },
  {
    id: "sql-13",
    category: "SQL",
    question: "RANK vs DENSE_RANK — what's the difference?",
    answer: "Both are window functions for ranking. RANK: Skips rank numbers after ties. If two rows tie at rank 1, the next rank is 3 (skips 2). DENSE_RANK: Does NOT skip. If two rows tie at rank 1, the next rank is 2. Example with salaries [100, 100, 90]: RANK gives 1, 1, 3. DENSE_RANK gives 1, 1, 2. ROW_NUMBER gives unique ranks (1, 2, 3) even for ties. Use DENSE_RANK for 'find Nth highest' queries.",
    difficulty: "Medium",
    tags: ["sql", "window-functions", "ranking"],
  },
  {
    id: "sql-14",
    category: "SQL",
    question: "CHAR vs VARCHAR — what's the difference?",
    answer: "CHAR(n): Fixed-length. Always stores exactly n characters (pads with spaces if shorter). Faster for fixed-size data. Use for codes: `CHAR(2)` for country codes, `CHAR(10)` for phone numbers. VARCHAR(n): Variable-length. Stores 1 to n characters + 1-2 bytes for length. More storage-efficient for varying-length data. Use for names, emails, addresses. CHAR wastes space for variable data; VARCHAR has slight overhead for fixed data.",
    difficulty: "Easy",
    tags: ["sql", "data-types", "fundamentals"],
  },
  {
    id: "sql-15",
    category: "SQL",
    question: "InnoDB vs MyISAM — what's the difference?",
    answer: "InnoDB (default in MySQL): Supports transactions (ACID), row-level locking, foreign keys, crash recovery, MVCC (Multi-Version Concurrency Control). Better for write-heavy, concurrent workloads. MyISAM: No transactions, table-level locking, no foreign keys, full-text search (older MySQL). Faster for read-heavy, simple workloads. Counts rows faster (stores count). Modern recommendation: Always use InnoDB. MyISAM is legacy.",
    difficulty: "Medium",
    tags: ["sql", "mysql", "storage-engines"],
  },
  {
    id: "sql-16",
    category: "SQL",
    question: "What are Magic Tables in SQL?",
    answer: "Magic tables are special temporary tables available inside triggers: INSERTED (contains new rows for INSERT/UPDATE operations) and DELETED (contains old rows for DELETE/UPDATE operations). For INSERT trigger: INSERTED has new rows. For DELETE trigger: DELETED has removed rows. For UPDATE trigger: DELETED has old values, INSERTED has new values. Used for auditing, validation, cascading logic. SQL Server terminology — MySQL uses NEW/OLD row references.",
    difficulty: "Medium",
    tags: ["sql", "triggers", "advanced"],
  },

  // =====================================================
  // JAVA
  // =====================================================
  {
    id: "java-1",
    category: "Java",
    question: "What are the differences between JDK, JRE, and JVM?",
    answer: "JVM (Java Virtual Machine): Runtime engine that executes bytecode. Platform-specific. Handles memory management, garbage collection, security. JRE (Java Runtime Environment): JVM + core libraries (java.lang, java.util, etc.) needed to RUN Java programs. JDK (Java Development Kit): JRE + development tools (javac compiler, debugger, jar, javadoc). JDK ⊃ JRE ⊃ JVM. To develop: install JDK. To run: install JRE.",
    difficulty: "Easy",
    tags: ["java", "jdk", "jvm", "fundamentals"],
  },
  {
    id: "java-2",
    category: "Java",
    question: "How does Java achieve platform independence?",
    answer: "Java source code (.java) is compiled by javac into platform-independent bytecode (.class files). This bytecode runs on the JVM, which IS platform-specific — there's a different JVM for Windows, macOS, Linux. The JVM interprets/JIT-compiles bytecode into native machine instructions. This 'Write Once, Run Anywhere' (WORA) approach means the same .class file runs on any OS with a compatible JVM installed.",
    difficulty: "Easy",
    tags: ["java", "bytecode", "jvm"],
  },
  {
    id: "java-3",
    category: "Java",
    question: "Explain OOP principles (Encapsulation, Abstraction, Inheritance, Polymorphism) with examples.",
    answer: "Encapsulation: Bundle data + methods, restrict access via private fields + public getters/setters. Abstraction: Hide complex implementation, expose simple interface (abstract classes, interfaces). Inheritance: `class Dog extends Animal` — reuse parent's fields/methods, extend behavior. Polymorphism: Same method, different behavior. Compile-time (method overloading — same name, different params) and Runtime (method overriding — child overrides parent's method, resolved via virtual dispatch).",
    difficulty: "Easy",
    tags: ["java", "oop", "fundamentals"],
  },
  {
    id: "java-4",
    category: "Java",
    question: "Difference between abstract class and interface in Java?",
    answer: "Abstract class: Can have state (fields), constructors, abstract + concrete methods. Single inheritance only. Use when subclasses share common state/behavior. Interface (Java 8+): Can have abstract, default, and static methods. No instance state (only constants). Multiple inheritance supported. Use for contracts/capabilities. Key change in Java 8: interfaces got default methods, blurring the distinction. Rule: Use interface for 'can-do' (Serializable), abstract class for 'is-a' with shared state.",
    difficulty: "Easy",
    tags: ["java", "oop", "abstract", "interface"],
  },
  {
    id: "java-5",
    category: "Java",
    question: "How does Java handle memory management (Heap, Stack, Garbage Collection)?",
    answer: "Stack: Method frames, local variables, references. Thread-specific, LIFO, auto-freed when method returns. Heap: Objects and instance variables. Shared across threads. Divided into Young Gen (Eden + Survivor spaces) and Old Gen. Garbage Collection: JVM automatically reclaims unreachable objects. Young Gen uses minor GC (fast, frequent). Old Gen uses major GC (slower). Collectors: Serial, Parallel, G1 (default), ZGC (low-latency). No manual memory management (no free/delete).",
    difficulty: "Hard",
    tags: ["java", "memory", "gc"],
  },
  {
    id: "java-6",
    category: "Java",
    question: "Difference between String, StringBuilder, and StringBuffer?",
    answer: "String: Immutable. Every modification creates a new object. Thread-safe. Stored in String Pool for reuse. StringBuilder: Mutable. NOT thread-safe. Use for string manipulation in single-threaded code. Fast. StringBuffer: Mutable. Thread-safe (synchronized methods). Slower than StringBuilder due to synchronization overhead. Use: String for constants/keys, StringBuilder for building strings (loops, concatenation), StringBuffer only when thread safety is needed.",
    difficulty: "Easy",
    tags: ["java", "strings", "fundamentals"],
  },
  {
    id: "java-7",
    category: "Java",
    question: "What are checked vs unchecked exceptions in Java?",
    answer: "Checked: Compiler forces you to handle them (try-catch or throws). Subclasses of Exception (not RuntimeException). Examples: IOException, SQLException, FileNotFoundException. Unchecked: Not checked at compile time. Subclasses of RuntimeException. Examples: NullPointerException, ArrayIndexOutOfBoundsException, ArithmeticException. Errors (OutOfMemoryError, StackOverflowError) are also unchecked but indicate JVM problems. Best practice: Use checked for recoverable conditions, unchecked for programming errors.",
    difficulty: "Medium",
    tags: ["java", "exceptions", "error-handling"],
  },
  {
    id: "java-8",
    category: "Java",
    question: "What are functional interfaces and lambdas in Java 8?",
    answer: "A functional interface has exactly ONE abstract method (can have default/static methods). Annotated with @FunctionalInterface. Built-in: Predicate<T> (test), Function<T,R> (apply), Consumer<T> (accept), Supplier<T> (get). Lambdas provide concise syntax for implementing them: `Predicate<String> isEmpty = s -> s.isEmpty();` instead of anonymous class. Method references (::) are shorthand: `String::isEmpty`. Enables functional programming patterns in Java.",
    difficulty: "Medium",
    tags: ["java", "java8", "lambdas", "functional"],
  },
  {
    id: "java-9",
    category: "Java",
    question: "Difference between process vs thread?",
    answer: "Process: Independent program instance with its own memory space (heap, code, stack). Heavy to create. Inter-process communication (IPC) via pipes, sockets, shared memory. Thread: Lightweight unit of execution within a process. Shares process memory (heap, code) but has its own stack. Fast to create/context-switch. Threads within the same process can communicate directly via shared memory (requires synchronization). A process has at least one thread (main thread).",
    difficulty: "Easy",
    tags: ["java", "threads", "concurrency"],
  },
  {
    id: "java-10",
    category: "Java",
    question: "What are the states of a Thread lifecycle?",
    answer: "NEW: Thread created but not started. RUNNABLE: After start() — ready to run or running (OS decides). BLOCKED: Waiting to acquire a monitor lock (enter synchronized block). WAITING: Indefinitely waiting (Object.wait(), Thread.join(), LockSupport.park()). TIMED_WAITING: Waiting with timeout (Thread.sleep(ms), Object.wait(ms)). TERMINATED: Execution completed or exception thrown. Use Thread.getState() to check. A terminated thread cannot be restarted.",
    difficulty: "Medium",
    tags: ["java", "threads", "lifecycle"],
  },
  {
    id: "java-11",
    category: "Java",
    question: "Explain synchronized vs ReentrantLock.",
    answer: "synchronized: Built-in keyword. Implicit lock/unlock. Block or method level. Non-interruptible waiting. No try-lock. Automatic unlock on exception. ReentrantLock: java.util.concurrent.locks. Explicit lock()/unlock() in try-finally. Supports tryLock() (non-blocking), lockInterruptibly(), fair locking, multiple Condition objects. More flexible but verbose. Both are reentrant (same thread can acquire lock multiple times). Use synchronized for simple cases; ReentrantLock for advanced control.",
    difficulty: "Hard",
    tags: ["java", "concurrency", "locks"],
  },
  {
    id: "java-12",
    category: "Java",
    question: "How does HashMap work internally (hashing, collision, resizing)?",
    answer: "HashMap uses an array of buckets (Node[]). When you put(key, value): 1) Compute hash: key.hashCode() → spread bits → index via (n-1) & hash, 2) If bucket empty, insert Node, 3) If collision (same index), chain as linked list (Java 7) or tree (Java 8, when chain length > 8). Get: same hashing → find bucket → traverse chain/tree with equals(). Resizing: When size > capacity × loadFactor (0.75), array doubles and entries are rehashed. O(1) average, O(n) worst case.",
    difficulty: "Hard",
    tags: ["java", "collections", "hashmap"],
  },
  {
    id: "java-13",
    category: "Java",
    question: "Difference between ArrayList vs LinkedList?",
    answer: "ArrayList: Dynamic array. O(1) random access (get/set by index). O(n) insert/delete in middle (shifts elements). Good for read-heavy workloads. Less memory overhead. LinkedList: Doubly-linked list. O(n) random access (must traverse). O(1) insert/delete at known position (no shifting). Implements Deque. More memory (each node stores prev/next pointers). In practice, ArrayList is faster for most use cases due to CPU cache locality.",
    difficulty: "Medium",
    tags: ["java", "collections", "list"],
  },
  {
    id: "java-14",
    category: "Java",
    question: "Explain Streams API and its advantages in Java 8.",
    answer: "Streams provide a functional, declarative way to process collections. `list.stream().filter(x -> x > 5).map(x -> x * 2).sorted().collect(Collectors.toList())`. Advantages: 1) Concise, readable code, 2) Lazy evaluation (intermediate operations aren't executed until terminal operation), 3) Easy parallelism (`parallelStream()`), 4) Supports operations: filter, map, flatMap, reduce, collect, groupingBy, partitioningBy. Streams don't modify the source collection (immutable pipeline).",
    difficulty: "Medium",
    tags: ["java", "java8", "streams"],
  },
  {
    id: "java-15",
    category: "Java",
    question: "Difference between Spring vs Spring Boot?",
    answer: "Spring: Comprehensive framework for Java enterprise apps. Requires extensive XML/Java configuration, manual dependency management, server setup. Spring Boot: Opinionated layer on top of Spring. Auto-configuration (scans classpath and configures beans automatically). Embedded server (Tomcat/Jetty). Starter dependencies (`spring-boot-starter-web`, `spring-boot-starter-data-jpa`). Production-ready features (actuator, health checks). Minimal boilerplate. Boot makes Spring development 10x faster.",
    difficulty: "Easy",
    tags: ["java", "spring", "spring-boot"],
  },
  {
    id: "java-16",
    category: "Java",
    question: "How does Dependency Injection (DI) work in Spring?",
    answer: "DI is an IoC pattern where Spring creates and injects dependencies rather than objects creating their own. Types: Constructor injection (recommended — immutable, testable), Setter injection, Field injection (@Autowired — discouraged, hard to test). Spring's IoC container scans for beans (@Component, @Service, @Repository, @Controller), creates them, resolves dependencies, and injects them. Configuration via annotations, Java config (@Bean), or XML. Promotes loose coupling and testability.",
    difficulty: "Medium",
    tags: ["java", "spring", "di"],
  },
  {
    id: "java-17",
    category: "Java",
    question: "How do you secure REST APIs in Spring Boot (Spring Security / JWT)?",
    answer: "1) Add spring-boot-starter-security, 2) Configure SecurityFilterChain: disable CSRF for APIs, set stateless session management, define URL authorization rules, 3) Create JWT utility class (generate/validate tokens with a secret), 4) Build JwtAuthenticationFilter extending OncePerRequestFilter — extract token from Authorization header, validate, set SecurityContext, 5) Add the filter before UsernamePasswordAuthenticationFilter. Optional: Use OAuth2 Resource Server for standard JWT validation.",
    difficulty: "Hard",
    tags: ["java", "spring-security", "jwt"],
  },
  {
    id: "java-18",
    category: "Java",
    question: "What is Deadlock? How to detect and prevent it?",
    answer: "Deadlock: Two+ threads each hold a lock the other needs — both wait forever. Conditions (all 4 needed): mutual exclusion, hold and wait, no preemption, circular wait. Detection: Thread dumps (jstack), JMX, JVisualVM show blocked threads. Prevention: 1) Lock ordering (always acquire locks in same global order), 2) tryLock with timeout, 3) Avoid nested locks, 4) Use java.util.concurrent (ConcurrentHashMap, CopyOnWriteArrayList) instead of manual synchronization.",
    difficulty: "Hard",
    tags: ["java", "concurrency", "deadlock"],
  },
  {
    id: "java-19",
    category: "Java",
    question: "What is Boxing and Unboxing in Java?",
    answer: "Boxing: Converting a primitive to its wrapper object. `int` → `Integer`, `double` → `Double`. Example: `Integer obj = 42;` (autoboxing). Unboxing: Converting a wrapper object back to primitive. `Integer obj = 42; int prim = obj;` (autounboxing). Performance pitfall: Autoboxing in loops creates many objects. `Integer` cache: values -128 to 127 are cached, so `Integer.valueOf(127) == Integer.valueOf(127)` is true, but `new Integer(127) == new Integer(127)` is false.",
    difficulty: "Easy",
    tags: ["java", "primitives", "wrapper"],
  },

  // =====================================================
  // PYTHON
  // =====================================================
  {
    id: "py-1",
    category: "Python",
    question: "Difference between list, tuple, set, and dictionary?",
    answer: "List: Ordered, mutable, allows duplicates. `[1, 2, 3]`. O(1) index access. Tuple: Ordered, immutable, allows duplicates. `(1, 2, 3)`. Hashable (can be dict key). Set: Unordered, mutable, NO duplicates. `{1, 2, 3}`. O(1) membership test. Dictionary: Key-value pairs, ordered (Python 3.7+), mutable, keys must be unique and hashable. `{'a': 1}`. O(1) access by key. Use list for ordered collections, tuple for immutable data, set for uniqueness, dict for key-value mapping.",
    difficulty: "Easy",
    tags: ["python", "data-structures", "fundamentals"],
  },
  {
    id: "py-2",
    category: "Python",
    question: "Explain Python's GIL (Global Interpreter Lock).",
    answer: "The GIL is a mutex in CPython that allows only ONE thread to execute Python bytecode at a time. This means multi-threaded Python programs don't get true parallelism for CPU-bound tasks. Exists because CPython's memory management (reference counting) is not thread-safe. Workarounds: 1) multiprocessing module (true parallelism, separate processes), 2) asyncio for I/O-bound concurrency, 3) C extensions that release the GIL, 4) Use PyPy or other GIL-free interpreters.",
    difficulty: "Hard",
    tags: ["python", "gil", "concurrency"],
  },
  {
    id: "py-3",
    category: "Python",
    question: "Explain Python decorators with an example.",
    answer: "A decorator wraps a function to extend its behavior without modifying it. It's a function that takes a function and returns a new function. Example: `def timer(func): def wrapper(*args, **kwargs): start = time.time(); result = func(*args, **kwargs); print(f'{time.time()-start}s'); return result; return wrapper`. Usage: `@timer` above a function definition. Built-in decorators: @staticmethod, @classmethod, @property, @functools.lru_cache.",
    difficulty: "Medium",
    tags: ["python", "decorators", "functions"],
  },
  {
    id: "py-4",
    category: "Python",
    question: "What is a generator? How does yield work?",
    answer: "A generator is a function that uses `yield` instead of `return`. It produces values lazily, one at a time, saving memory. When called, it returns a generator object (iterator). Each `next()` call executes until the next `yield`, returning the value and suspending state. Example: `def fib(): a, b = 0, 1; while True: yield a; a, b = b, a+b`. Usage: `gen = fib(); next(gen) # 0, next(gen) # 1`. Generator expressions: `(x*2 for x in range(10))`.",
    difficulty: "Medium",
    tags: ["python", "generators", "iterators"],
  },
  {
    id: "py-5",
    category: "Python",
    question: "What is bias-variance tradeoff in Machine Learning?",
    answer: "Bias: Error from overly simplistic model assumptions (underfitting). High bias = model misses relevant patterns. Variance: Error from sensitivity to small fluctuations in training data (overfitting). High variance = model captures noise as patterns. Tradeoff: Increasing model complexity reduces bias but increases variance (and vice versa). Goal: Find the sweet spot (minimum total error). Techniques: Cross-validation, regularization (L1/L2), ensemble methods (bagging reduces variance, boosting reduces bias).",
    difficulty: "Medium",
    tags: ["python", "ml", "fundamentals"],
  },
  {
    id: "py-6",
    category: "Python",
    question: "What are AI Agents? Difference between an LLM and an Agent?",
    answer: "LLM: A language model that generates text based on input (stateless, single-turn). Agent: An LLM-powered system that can observe, reason, plan, and take ACTIONS using tools (APIs, databases, search, code execution) in a loop until a goal is achieved. Agents have: 1) Perception (understand input), 2) Reasoning (chain-of-thought), 3) Action (call tools), 4) Memory (conversation history, vector stores). Frameworks: LangChain, AutoGPT, CrewAI. Key difference: Agents ACT on the world; LLMs only generate text.",
    difficulty: "Medium",
    tags: ["python", "ai", "agents"],
  },
  {
    id: "py-7",
    category: "Python",
    question: "Explain RAG (Retrieval-Augmented Generation).",
    answer: "RAG combines a retrieval system with a generative LLM. Process: 1) Index documents by splitting into chunks and creating vector embeddings (stored in a vector DB like Pinecone, Weaviate, ChromaDB), 2) At query time, embed the user's question, 3) Retrieve top-K similar chunks via similarity search (cosine/dot product), 4) Inject retrieved context into the LLM prompt, 5) LLM generates a grounded answer. Benefits: Reduces hallucination, uses private/current data without fine-tuning, verifiable sources.",
    difficulty: "Hard",
    tags: ["python", "rag", "genai"],
  },
  {
    id: "py-8",
    category: "Python",
    question: "What is Django ORM? How does it compare to raw SQL?",
    answer: "Django ORM maps Python classes to database tables. `User.objects.filter(age__gte=18).order_by('-name')` generates SQL automatically. Advantages: Pythonic, database-agnostic, prevents SQL injection, migrations built-in. vs Raw SQL: ORM adds overhead, complex queries can be hard to express, generated SQL may be suboptimal. Use ORM for standard CRUD; raw SQL (`Model.objects.raw()` or `connection.cursor()`) for complex reporting queries. select_related (JOIN) vs prefetch_related (separate query) optimize related lookups.",
    difficulty: "Medium",
    tags: ["python", "django", "orm"],
  },
  {
    id: "py-9",
    category: "Python",
    question: "Difference between Django REST Framework and FastAPI?",
    answer: "DRF: Built on Django (full-featured, batteries-included). Serializers, viewsets, routers, auth, admin panel. Synchronous. Mature ecosystem. Best for complex apps needing full Django features. FastAPI: Modern, async-first (Starlette + Pydantic). Type-hint-driven validation, auto-generated OpenAPI docs, high performance (comparable to Node/Go). Best for microservices, ML APIs, high-performance endpoints. DRF = feature-rich monolith; FastAPI = lean, fast, modern.",
    difficulty: "Medium",
    tags: ["python", "django", "fastapi"],
  },
  {
    id: "py-10",
    category: "Python",
    question: "How does FastAPI handle validation (Pydantic models)?",
    answer: "FastAPI uses Pydantic models for automatic request/response validation. Define a model: `class User(BaseModel): name: str; age: int = Field(ge=0); email: EmailStr`. Use in endpoint: `@app.post('/users') async def create(user: User):`. FastAPI automatically: 1) Parses request body as JSON, 2) Validates against the Pydantic model, 3) Returns 422 with detailed errors on validation failure, 4) Generates OpenAPI schema. Also validates query params, path params, and headers via type hints.",
    difficulty: "Medium",
    tags: ["python", "fastapi", "pydantic"],
  },

  // =====================================================
  // .NET (C#, .NET Core, .NET Framework)
  // =====================================================
  {
    id: "dotnet-1",
    category: ".NET",
    question: "What are Collections and types of collections in C#?",
    answer: "Non-generic (System.Collections): ArrayList, Hashtable, Stack, Queue — store objects, no type safety, boxing overhead. Generic (System.Collections.Generic): List<T>, Dictionary<TKey,TValue>, Stack<T>, Queue<T>, HashSet<T> — type-safe, no boxing, better performance. Concurrent (System.Collections.Concurrent): ConcurrentDictionary, ConcurrentQueue, BlockingCollection — thread-safe. Immutable (System.Collections.Immutable): ImmutableList, ImmutableDictionary — cannot be modified after creation. Always prefer generic collections.",
    difficulty: "Easy",
    tags: ["dotnet", "collections", "csharp"],
  },
  {
    id: "dotnet-2",
    category: ".NET",
    question: "What is LINQ in C#?",
    answer: "Language Integrated Query — a unified syntax for querying data from any source (collections, databases, XML). Query syntax: `from u in users where u.Age > 18 select u.Name`. Method syntax: `users.Where(u => u.Age > 18).Select(u => u.Name)`. Key operators: Where, Select, OrderBy, GroupBy, Join, Aggregate, Any, All, First, Single, Skip, Take. LINQ to Objects (in-memory), LINQ to Entities (EF Core → SQL), LINQ to XML. Deferred execution — queries execute on enumeration.",
    difficulty: "Medium",
    tags: ["dotnet", "linq", "csharp"],
  },
  {
    id: "dotnet-3",
    category: ".NET",
    question: "Value types vs Reference types in C#?",
    answer: "Value types: Stored on the stack (or inline). Contain actual data. Copied on assignment. Types: int, double, bool, char, struct, enum. Default: 0/false. Reference types: Stored on the heap, stack holds a reference (pointer). Assignment copies the reference (both point to same object). Types: class, string, array, delegate, interface. Default: null. Structs are value types (good for small, immutable data); Classes are reference types (for complex objects with behavior).",
    difficulty: "Medium",
    tags: ["dotnet", "csharp", "types"],
  },
  {
    id: "dotnet-4",
    category: ".NET",
    question: "Abstract class vs Interface in C#?",
    answer: "Abstract class: Can have fields, constructors, abstract + concrete methods. Single inheritance. Use for 'is-a' relationships with shared state. Interface (C# 8+): Can have default implementations, static methods. No fields (only properties). Multiple inheritance supported. Use for 'can-do' contracts. Key difference: A class can implement multiple interfaces but inherit only one abstract class. C# 8 default interface methods blur the line but interfaces still can't have instance state.",
    difficulty: "Easy",
    tags: ["dotnet", "oop", "csharp"],
  },
  {
    id: "dotnet-5",
    category: ".NET",
    question: "const vs readonly vs static in C#?",
    answer: "const: Compile-time constant. Must be assigned at declaration. Implicitly static. Only primitive types and strings. Inlined at compilation (changing requires recompilation of dependents). readonly: Runtime constant. Can be assigned at declaration OR in constructor. Instance or static. Any type. static: Belongs to the type, not instances. One copy shared. Use const for true constants (Math.PI), readonly for values determined at runtime (config), static for shared data/methods.",
    difficulty: "Medium",
    tags: ["dotnet", "csharp", "modifiers"],
  },
  {
    id: "dotnet-6",
    category: ".NET",
    question: "Explain async/await in C#.",
    answer: "async marks a method as asynchronous, returning Task or Task<T>. await pauses execution until the awaited Task completes, without blocking the thread. The thread is released back to the pool. When the Task completes, execution resumes (possibly on a different thread). Example: `async Task<string> GetDataAsync() { var response = await httpClient.GetAsync(url); return await response.Content.ReadAsStringAsync(); }`. Don't use async void (except event handlers). Use ConfigureAwait(false) in libraries.",
    difficulty: "Medium",
    tags: ["dotnet", "async", "csharp"],
  },
  {
    id: "dotnet-7",
    category: ".NET",
    question: "Filters vs Middleware in ASP.NET Core?",
    answer: "Middleware: Runs on EVERY request in the pipeline (order matters). Handles cross-cutting concerns: logging, CORS, authentication, error handling. Configured in Program.cs with `app.Use()`. Has no access to MVC-specific context. Filters: Run only for MVC requests that reach the action. Types: Authorization, Resource, Action, Exception, Result filters. Have access to ActionContext, ModelState. Use middleware for global concerns; filters for MVC-specific concerns (validation, caching, authorization per-action).",
    difficulty: "Medium",
    tags: ["dotnet", "aspnet", "middleware"],
  },
  {
    id: "dotnet-8",
    category: ".NET",
    question: ".NET Framework vs .NET Core — what's the difference?",
    answer: ".NET Framework: Windows-only, mature, full API surface (WPF, WinForms, WCF, WebForms). No longer actively developed (4.8 is last). .NET Core (now .NET 5/6/7/8/9): Cross-platform (Windows/Linux/macOS), high performance, modular (NuGet packages), open source, cloud-optimized, supports microservices/containers. .NET 5+ unified both into just '.NET'. New projects should always use modern .NET (8/9+). Migration from Framework to modern .NET is well-documented.",
    difficulty: "Easy",
    tags: ["dotnet", "framework", "core"],
  },
  {
    id: "dotnet-9",
    category: ".NET",
    question: "IActionResult vs ActionResult in ASP.NET Core?",
    answer: "IActionResult: Interface. Can return ANY action result type (Ok(), NotFound(), BadRequest(), File(), View()). Most flexible. ActionResult<T>: Generic. Can return either the type T directly OR an action result. Enables better Swagger/OpenAPI documentation because the response type is known. Example: `ActionResult<User>` can return `Ok(user)` or `NotFound()`. Use ActionResult<T> for typed API endpoints; IActionResult when return type varies significantly.",
    difficulty: "Medium",
    tags: ["dotnet", "aspnet", "api"],
  },
  {
    id: "dotnet-10",
    category: ".NET",
    question: "What is Kestrel in .NET Core?",
    answer: "Kestrel is the default, cross-platform, high-performance web server built into ASP.NET Core. It's based on libuv (and later, custom socket transport). Can be used as: 1) Edge server (directly internet-facing) with HTTPS, 2) Behind a reverse proxy (Nginx, IIS, Apache) for production (adds features like load balancing, static file serving, SSL termination). Kestrel handles millions of requests per second. Configured in Program.cs via `builder.WebHost.ConfigureKestrel()`.",
    difficulty: "Medium",
    tags: ["dotnet", "kestrel", "aspnet"],
  },
  {
    id: "dotnet-11",
    category: ".NET",
    question: "What are Design Patterns commonly used in .NET?",
    answer: "Creational: Singleton (one instance — `services.AddSingleton`), Factory (create objects without specifying class), Builder (step-by-step construction). Structural: Repository (abstract data access), Adapter (convert interfaces), Decorator (wrap to extend behavior). Behavioral: Observer (events/delegates in C#), Strategy (swap algorithms via interfaces), Mediator (MediatR for CQRS). .NET DI container itself implements IoC/DI pattern. Factory pattern return type is typically an interface or abstract class.",
    difficulty: "Medium",
    tags: ["dotnet", "design-patterns", "architecture"],
  },
  {
    id: "dotnet-12",
    category: ".NET",
    question: "IEnumerable vs IEnumerator in C#?",
    answer: "IEnumerable<T>: Exposes `GetEnumerator()` method. Represents a sequence you can iterate over with foreach. Supports deferred execution (LINQ). IEnumerator<T>: The actual iterator with `Current`, `MoveNext()`, `Reset()`. Maintains iteration state. Relationship: IEnumerable creates an IEnumerator. Analogy: IEnumerable is the book; IEnumerator is the bookmark. You typically use IEnumerable (foreach handles IEnumerator internally). Implement IEnumerable for custom collections.",
    difficulty: "Medium",
    tags: ["dotnet", "csharp", "collections"],
  },
  {
    id: "dotnet-13",
    category: ".NET",
    question: "What are == vs .Equals() in C#?",
    answer: "For value types: Both compare values. `5 == 5` and `5.Equals(5)` both true. For reference types: `==` compares references by default (are they the same object?). `.Equals()` defaults to reference equality but can be overridden (and IS overridden by String, DateTime, etc.). `string a = \"hi\"; string b = \"hi\"; a == b` → true (String overloads ==). For custom classes, override Equals() and GetHashCode() together, and optionally overload ==. `ReferenceEquals()` always checks identity.",
    difficulty: "Medium",
    tags: ["dotnet", "csharp", "equality"],
  },
  {
    id: "dotnet-14",
    category: ".NET",
    question: "What is CLR, GAC, and AppDomain?",
    answer: "CLR (Common Language Runtime): The virtual machine that manages .NET code execution — JIT compilation, garbage collection, type safety, exception handling, security. GAC (Global Assembly Cache): Machine-wide cache for shared .NET Framework assemblies. Eliminates DLL Hell by supporting side-by-side versioning. Less relevant in .NET Core (uses local deployment). AppDomain: Lightweight isolation boundary within a process. Allows loading/unloading assemblies independently. Replaced by AssemblyLoadContext in .NET Core.",
    difficulty: "Hard",
    tags: ["dotnet", "clr", "runtime"],
  },

  // =====================================================
  // ADO.NET
  // =====================================================
  {
    id: "ado-1",
    category: "ADO.NET",
    question: "Connected vs Disconnected architecture in ADO.NET?",
    answer: "Connected: Maintains an open database connection throughout operations. Uses SqlConnection, SqlCommand, SqlDataReader. DataReader provides forward-only, read-only access (fast, low memory). Connection must stay open while reading. Disconnected: Opens connection briefly to fill data, then closes. Uses SqlDataAdapter, DataSet, DataTable. Data is cached in memory, editable offline, changes pushed back via DataAdapter.Update(). Connected = fast reads; Disconnected = flexibility + offline editing.",
    difficulty: "Medium",
    tags: ["adonet", "architecture", "database"],
  },
  {
    id: "ado-2",
    category: "ADO.NET",
    question: "SqlDataReader vs SqlDataAdapter?",
    answer: "SqlDataReader: Connected, forward-only, read-only cursor. Very fast for sequential reads. Low memory footprint (one row at a time). Must manually manage connection. Best for reading large datasets sequentially. SqlDataAdapter: Bridge between disconnected DataSet/DataTable and database. Opens connection, executes query, fills DataTable, closes connection. Supports SelectCommand, InsertCommand, UpdateCommand, DeleteCommand. Best when you need to edit data in memory and push changes back.",
    difficulty: "Medium",
    tags: ["adonet", "datareader", "dataadapter"],
  },
  {
    id: "ado-3",
    category: "ADO.NET",
    question: "How to prevent SQL Injection in ADO.NET?",
    answer: "ALWAYS use parameterized queries: `cmd.CommandText = \"SELECT * FROM Users WHERE Id = @id\"; cmd.Parameters.AddWithValue(\"@id\", userId);`. NEVER concatenate user input into SQL strings. Additional measures: Use stored procedures with parameters, validate/sanitize input, use ORM (Entity Framework), apply least-privilege database permissions, use connection string with minimum required permissions. Parameterized queries separate SQL code from data, preventing malicious input from being executed as SQL.",
    difficulty: "Medium",
    tags: ["adonet", "security", "sql-injection"],
  },
  {
    id: "ado-4",
    category: "ADO.NET",
    question: "How do Transactions work in ADO.NET?",
    answer: "Transactions ensure ACID compliance for multiple operations. Usage: `using var conn = new SqlConnection(connStr); conn.Open(); using var tx = conn.BeginTransaction(); try { var cmd1 = new SqlCommand(sql1, conn, tx); cmd1.ExecuteNonQuery(); var cmd2 = new SqlCommand(sql2, conn, tx); cmd2.ExecuteNonQuery(); tx.Commit(); } catch { tx.Rollback(); throw; }`. All commands succeed together or all roll back. For distributed transactions across databases, use TransactionScope.",
    difficulty: "Medium",
    tags: ["adonet", "transactions", "acid"],
  },

  // =====================================================
  // SOLUTION ARCHITECTURE / SYSTEM DESIGN
  // =====================================================
  {
    id: "arch-1",
    category: "System Design",
    question: "Microservice vs Monolithic architecture?",
    answer: "Monolithic: Single deployable unit. All features in one codebase. Simple to develop, test, deploy initially. Downsides: scaling is all-or-nothing, one bug can take down everything, tech stack locked, slow CI/CD as it grows. Microservices: Each feature is an independent service with its own database, deployed separately. Benefits: independent scaling, tech diversity, team autonomy, fault isolation. Challenges: distributed system complexity (networking, data consistency, debugging, service discovery, API gateway).",
    difficulty: "Medium",
    tags: ["architecture", "microservices", "monolith"],
  },
  {
    id: "arch-2",
    category: "System Design",
    question: "Micro Frontend Architecture — what is it?",
    answer: "Extending microservices to the frontend. Each team owns an end-to-end vertical slice (UI + API + DB). The main app shell composes separately deployed frontend modules. Approaches: 1) iframe-based (simple, isolated, poor UX), 2) Web Components, 3) Module Federation (Webpack 5 — share code at runtime), 4) Single-SPA (framework-agnostic orchestrator). Benefits: team autonomy, independent deployment, tech diversity per micro-frontend. Challenges: shared state, consistent UX, bundle size, routing.",
    difficulty: "Hard",
    tags: ["architecture", "micro-frontend", "scaling"],
  },
  {
    id: "arch-3",
    category: "System Design",
    question: "Scenario: How to create a Movie Booking app with block/unblock seats?",
    answer: "Architecture: 1) Seat Service: Maps theater → screens → seats with status (available/blocked/booked). 2) Blocking: Use time-bound pessimistic locking — when user selects seats, mark as 'blocked' with TTL (e.g., 10 min) using Redis SET with expiry. Other users see seats as unavailable. 3) Booking: On payment success, atomically change status from blocked→booked in DB (use transaction). 4) Unblock: TTL expiry in Redis auto-releases, or background job checks expired blocks. 5) Concurrency: Use optimistic locking (version field) or distributed lock (Redlock) to prevent double-booking.",
    difficulty: "Hard",
    tags: ["system-design", "scenario", "booking"],
  },
  {
    id: "arch-4",
    category: "System Design",
    question: "When to use MongoDB over SQL?",
    answer: "Use MongoDB when: 1) Schema is flexible or evolving rapidly (prototyping), 2) Data is naturally hierarchical/nested (documents), 3) Need horizontal scaling (sharding built-in), 4) High write throughput needed, 5) Geospatial queries, 6) Real-time analytics. Use SQL when: Complex relationships (JOINs), ACID transactions across multiple tables, strict data integrity, complex reporting, well-defined schema. Many modern apps use both (polyglot persistence) — SQL for transactions, MongoDB for content/logs.",
    difficulty: "Medium",
    tags: ["system-design", "mongodb", "sql"],
  },
  {
    id: "arch-5",
    category: "System Design",
    question: "What is the SOLID principle?",
    answer: "S — Single Responsibility: A class should have one reason to change. O — Open/Closed: Open for extension, closed for modification (use interfaces/inheritance). L — Liskov Substitution: Subtypes must be substitutable for their base types without breaking behavior. I — Interface Segregation: Many specific interfaces are better than one general one. D — Dependency Inversion: Depend on abstractions (interfaces) not concretions (classes). SOLID produces maintainable, testable, flexible codebases.",
    difficulty: "Medium",
    tags: ["system-design", "solid", "principles"],
  },
  {
    id: "arch-6",
    category: "System Design",
    question: "SQS, SNS — and alternatives to Kafka?",
    answer: "AWS SQS (Simple Queue Service): Fully managed message queue. Point-to-point. At-least-once delivery. Good for decoupling services. AWS SNS (Simple Notification Service): Pub/sub. Sends messages to multiple subscribers (SQS, Lambda, email, HTTP). SNS+SQS fan-out is a common pattern. Kafka alternatives: AWS Kinesis (managed streaming), Apache Pulsar (multi-tenant, tiered storage), RabbitMQ (traditional message broker, supports multiple protocols), Redis Streams (lightweight). Choose based on throughput, ordering, retention, and managed vs self-hosted needs.",
    difficulty: "Hard",
    tags: ["system-design", "messaging", "aws"],
  },
  {
    id: "arch-7",
    category: "System Design",
    question: "How and why use different types of Data Structures?",
    answer: "Array/List: O(1) index access, ordered. Use for sequential data. Stack (LIFO): Undo operations, parsing, DFS. Queue (FIFO): BFS, task scheduling, message queues. HashMap/Dictionary: O(1) key-value lookup. Caching, counting, dedup. Set: O(1) membership test, uniqueness. Tree: Hierarchical data, BST for sorted data, Trie for prefix search. Graph: Networks, social connections, routing. Heap: Priority queues, top-K elements. Choose based on the operations you need to optimize (search, insert, delete, ordered traversal).",
    difficulty: "Medium",
    tags: ["system-design", "data-structures", "fundamentals"],
  },
];

/** Get unique question categories */
export const questionCategories: string[] = [
  ...new Set(interviewQuestions.map((q) => q.category)),
];
