name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - master
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - master

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
          lfs: false
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        env:
          VITE_API_BASE_URL: ${{ vars.VITE_API_BASE_URL }}
          VITE_APP_NAME: ${{ vars.VITE_APP_NAME }}
          VITE_USE_MOCK_DATA: ${{ vars.VITE_USE_MOCK_DATA }}
          VITE_GOOGLE_FORM_ACTION_URL: ${{ vars.VITE_GOOGLE_FORM_ACTION_URL }}
          VITE_GOOGLE_FORM_FIELD_NAME: ${{ vars.VITE_GOOGLE_FORM_FIELD_NAME }}
          VITE_GOOGLE_FORM_FIELD_EMAIL: ${{ vars.VITE_GOOGLE_FORM_FIELD_EMAIL }}
          VITE_GOOGLE_FORM_FIELD_PHONE: ${{ vars.VITE_GOOGLE_FORM_FIELD_PHONE }}
          VITE_GOOGLE_FORM_FIELD_EDUCATION: ${{ vars.VITE_GOOGLE_FORM_FIELD_EDUCATION }}
          VITE_GOOGLE_FORM_FIELD_WORK_EXPERIENCE: ${{ vars.VITE_GOOGLE_FORM_FIELD_WORK_EXPERIENCE }}
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_CALM_GRASS_03393B410 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/" # App source code path
          api_location: "api" # Api source code path - optional
          output_location: "dist" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_CALM_GRASS_03393B410 }}
          action: "close"
